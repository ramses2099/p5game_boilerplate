const W_WIDTH = 600;
const W_HEIGHT = 800;


function setup() {
 // put setup code here
 var canvas = createCanvas(W_WIDTH, W_HEIGHT);
 canvas.parent('cnv')
// createCanvas(W_WIDTH, W_HEIGHT);
 
 // Create a p5.Color object using a RGBA color string.
  let c3 = color('rgba(0, 0, 255, 1)');
  fill(c3);
  square(10, 55, 35);



}

function draw() {
  // put drawing code here
  
}
